const Event = require('./event.model');
const rest = require('../../util/http');

const events = (req, res, next) => {

};

const event = (req, res, next) => {

};

module.exports = {
  events,
  event
};
