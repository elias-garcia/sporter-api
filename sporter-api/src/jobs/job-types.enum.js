const jobTypes = {
  PASSWORD_RESET_TOKEN_EXPIRE: '[password reset token] expire token',
  EVENT_CONFIRM: '[event] confirm',
  EVENT_START: '[event] start',
  EVENT_FINISH: '[event] finish',
};

module.exports = jobTypes;
